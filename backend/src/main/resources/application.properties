spring.application.name=compilador-bd-backend
server.port=${PORT:8080}
app.cors.allowed-origins=${URL_FRONTEND:*}

# MySQL DataSource
spring.datasource.url=jdbc:mysql://localhost:3306/?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=#SQLaldo04
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.continue-on-error=true

spring.jackson.serialization.write-dates-as-timestamps=false
